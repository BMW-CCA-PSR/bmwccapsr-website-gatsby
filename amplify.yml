version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd web
        - yarn
    build:
      commands:
        - yarn run build
  artifacts:
    baseDirectory: ./web/public
    files:
        - '**/*'
  cache:
    paths:
        - node_modules/**/*